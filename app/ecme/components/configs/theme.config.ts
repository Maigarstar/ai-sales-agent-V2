import type { Theme } from '../@types/theme'

export const themeConfig = {
  mode: 'light',
  themeSchema: 'default',
  direction: 'ltr',
  panelExpand: false,
  layout: {
    type: 'modern',
    sideNavCollapse: false,
  },
} as Theme
