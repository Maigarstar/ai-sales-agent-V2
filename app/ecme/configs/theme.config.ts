import type { Theme } from '@/@types/theme'

export const themeConfig: Theme = {
  mode: 'light',
  direction: 'ltr',
  themeSchema: 'default',
  layout: {
    type: 'modern',
    sideNavCollapse: false,
  },
  panelExpand: false,
}
